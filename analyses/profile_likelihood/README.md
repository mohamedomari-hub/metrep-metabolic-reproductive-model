# Profile Likelihood

Profile-likelihood results used by the final portfolio are curated in
`results_final/` and interpreted in `docs/results_summary.md`.

The representative 3x3 profile-likelihood figure separates practically
identifiable, boundary-limited, and weak/flat parameters. Raw profile runs are
expensive and are not committed as generated output.

